{
  "name": "mitsu",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "nodemon ./bin/www",
    "prod:up": "NODE_ENV=production forever start ./bin/www",
    "prod:ls": "forever list",
    "prod:down": "forever stop ./bin/www",
    "make:model": "sequelize model:create --underscored",
    "migrate": "sequelize db:migrate --env development --config ./config/database.json",
    "migrate:fresh": "echo '' > database.sqlite3 & yarn migrate",
    "make:seeder": "sequelize seed:generate",
    "seed": "sequelize db:seed:all --config ./config/database.json"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "dotenv": "^8.2.0",
    "express": "~4.16.1",
    "forever": "^3.0.2",
    "http-errors": "~1.6.3",
    "jade": "~1.11.0",
    "morgan": "~1.9.1",
    "sequelize": "^6.3.5",
    "sequelize-cli": "^6.2.0",
    "serve-favicon": "^2.5.0",
    "sqlite3": "^5.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
